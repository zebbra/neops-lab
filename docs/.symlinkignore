# provide folders to ignore here (i.e., do not create symlinks for those)
# Symlinks are used to ensure that we never create a reference outside the docs folder to allow merging all docs
# together
# monitor/ carries the monitor's runtime config; no page includes from it
monitor
node_modules
submodules
cms
